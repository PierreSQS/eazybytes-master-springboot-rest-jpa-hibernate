spring.datasource.url=jdbc:mysql://eazybytesspringsecuritydb.co4b7agzqpmj.eu-central-1.rds.amazonaws.com/eazyschoolsection20db
spring.datasource.username=admin
spring.datasource.password=Galerien3?
spring.jpa.show-sql=true

# SQL-Logging
spring.jpa.properties.hibernate.format_sql=true

# Schema generated by Hibernate
# Do not run the schema.sql e.g. schema.sql no more needed!!!!!
spring.jpa.defer-datasource-initialization=true
spring.jpa.hibernate.ddl-auto=create
# Start the data.sql script
spring.sql.init.mode=ALWAYS

# prevents Validation in Spring JPA
spring.jpa.properties.javax.persistence.validation.mode=none

# prevents sometimes the eager loading of Data in the DB
# for example prevents the updating of Person with a new Role
# spring.jpa.open-in-view=false

# LOGGING-Properties
# Security
# logging.level.org.springframework.security=debug
# SQL-Binding
# logging.level.org.hibernate.type.descriptor.sql=trace

# Inside the framework
# debug=true
# trace=true

# Logging package specific
# logging.level.root=INFO
# logging.level.com.eazybytes.eazyschool.aspects = ERROR
# logging.level.com.eazybytes.eazyschool.controller = ERROR

# Initialize log group eazyschool_error
# logging.group.eazyschool_error=com.eazybytes.eazyschool.aspects, com.eazybytes.eazyschool.controller
# Set log level to log group eazyschool_error
# logging.level.eazyschool_error=ERROR
spring.output.ansi.enabled=ALWAYS

# Allows Bean name overriding when having Beans
# with the same name
spring.main.allow-bean-definition-overriding=true

# Base Path for Spring Data Rest
spring.data.rest.base-path=/data-rest
